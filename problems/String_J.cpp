#include <bits/stdc++.h>
using namespace std;
const int MXN = 1e5 + 3;
int n;
signed main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    string a, b, tmp;
    for(i=0;i<n;i++) {
        cin >> a >> b;
         
    }

    return 0;
}